[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["cd web && npm install", "cd server && npm install"]

[phases.build]
cmds = ["cd web && npm run build", "cd server && npm run build"]

[start]
cmd = "cd server && npm start"